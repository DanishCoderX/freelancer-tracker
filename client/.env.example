# For local dev, the proxy in package.json handles this automatically.
# Only set this when deploying to Vercel (point to your Render backend URL).
REACT_APP_API_URL=https://your-app.onrender.com/api
